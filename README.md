## Instagram Api (Node.js && Express.js && MongoDB)

# Instagram Api

## Open Endpoints

Open endpoints require no Authentication.

* [Posts]: `POST /posts` -- List all posts
* [Posts]: `GET /posts` -- Create one Post
* [Posts]: `GET /posts/:id/like` -- Add like for post
